import {
    createGame,
    playGame,
    activateSquareElement
} from '/src/js/game/game.js';

window.createGame = createGame;
window.playGame = playGame;
window.activateSquareElement = activateSquareElement;

createGame();